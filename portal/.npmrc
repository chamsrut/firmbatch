# Node 24 LTS, here and in CI, and `npm ci` refuses an older runtime rather than installing
# against it. Without this, `engines` in package.json is a warning nobody reads, and the
# portal gate would run on whatever Node happened to be on PATH.
engine-strict=true
